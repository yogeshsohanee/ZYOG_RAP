projection;
strict ( 2 );

define behavior for ZC_TRAVEL_YSN //alias <alias_name>
use etag
{
  use create;
  use update;
  use delete;

  use association _Booking { create; }
}

define behavior for ZC_BOOKING_YSN //alias <alias_name>
use etag
{
  use update;
  use delete;

  use association _Travel;
  use association _Booksuppl { create; }
}

define behavior for ZC_BOOKSUPPL_YSN //alias <alias_name>
use etag
{
  use update;
  use delete;

  use association _Travel;
  use association _Booking;
}